tags:
  - onboard
description: UPDATE - Reset Password
operationId: onboardResetPassword
parameters: []
requestBody:
  content:
    application/json:
      schema:
        required:
          - currentPassword
          - newPassword
        type: object
        properties:
          currentPassword:
            type: string
          newPassword:
            type: string
  required: true
responses:
  '200':
    description: OK
    content: {}
deprecated: false
security:
  - BearerAuth: []