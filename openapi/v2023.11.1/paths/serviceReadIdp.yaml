summary: READ - Read IdP
description: READ - Read IdP
operationId: serviceReadIdp
parameters:
  - $ref: "../parameters/idpId.yaml"
responses:
  "200":
    content:
      application/json:
        schema:
          type: object
          properties:
            idp:
              $ref: ../definitions/IDPExtras.yaml
    description: ""
  "400":
    description: bad input parameter
security:
  - BearerAuth: []
tags:
  - service
