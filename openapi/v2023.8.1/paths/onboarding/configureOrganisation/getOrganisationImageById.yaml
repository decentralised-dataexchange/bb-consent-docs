get:
  tags:
    - onboarding
  description: Get organization image
  parameters:
    - name: organizationId
      in: path
      required: true
      schema:
        type: string
    - name: imageId
      in: path
      required: true
      schema:
        type: string
  responses:
    "200":
      description: "OK"
      headers: {}
  deprecated: false
  security:
    - BearerAuth: []
