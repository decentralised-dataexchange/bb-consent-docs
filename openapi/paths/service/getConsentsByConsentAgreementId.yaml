get:
  tags:
    - service
  summary: Get consents by consent agreement ID
  parameters:
    - name: orgId
      in: path
      description: "Organisation ID"
      required: true
      schema:
        type: string
    - name: userId
      in: path
      description: "Individual ID"
      required: true
      schema:
        type: string
    - name: consentId
      in: path
      description: "Consent record ID"
      required: true
      schema:
        type: string
    - name: purposeId
      in: path
      description: "Consent agreement ID"
      required: true
      schema:
        type: string
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: "../../definitions/service/GetConsentsByConsentAgreementId.yaml"
  deprecated: false
  security:
    - BearerAuth: []
