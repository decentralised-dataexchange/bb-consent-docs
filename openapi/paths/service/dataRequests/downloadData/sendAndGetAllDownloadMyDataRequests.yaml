post:
  tags:
    - service
  summary: Send download my data request
  parameters:
    - name: orgId
      in: path
      required: true
      schema:
        type: string
  responses:
    "200":
      description: OK
      content: {}
    "400":
      description: Bad Request
      content:
        application/json:
          schema:
            $ref: "../../../../definitions/service/dataRequests/downloadData/SendDownloadMyDataRequestError.yaml"
  deprecated: false
  security:
    - BearerAuth: []
get:
  tags:
    - service
  summary: Get all download my data requests
  parameters:
    - name: orgId
      in: path
      required: true
      schema:
        type: string
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../../../../definitions/service/dataRequests/downloadData/GetAllDownloadMyDataRequests.yaml"
  deprecated: false
  security:
    - BearerAuth: []
