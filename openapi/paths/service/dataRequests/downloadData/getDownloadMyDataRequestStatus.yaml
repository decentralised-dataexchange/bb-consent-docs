get:
  tags:
    - service
  summary: Get download my data request status
  parameters:
    - name: orgId
      in: path
      required: true
      schema:
        type: string
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: "../../../../definitions/service/dataRequests/downloadData/GetDownloadMyDataRequestStatus.yaml"
  deprecated: false
  security:
    - BearerAuth: []
