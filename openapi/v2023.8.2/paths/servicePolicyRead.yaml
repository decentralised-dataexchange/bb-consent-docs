description: READ - fetches the latest version of a Policy and the presented revisionId
  of an associated Agreement
operationId: servicePolicyRead
parameters:
- description: Unique ID of an object
  in: path
  name: policyId
  required: true
  schema:
    type: string
- description: An object with id revisionId
  in: query
  name: revisionId
  required: false
  schema:
    type: string
responses:
  '200':
    content:
      application/json:
        schema:
          items:
            oneOf:
            - $ref: ../definitions/Policy.yaml
            - $ref: ../definitions/Revision.yaml
          type: array
    description: ''
  '400':
    description: bad input parameter
security:
  - BearerAuth: []
summary: READ - fetches the latest version of a Policy and the presented revisionId
  of an associated Agreement
tags:
- service
x-specification-crudl-model: Policy
x-specification-pii-or-sensitive: 'False'
x-specification-scenario: ''
x-specification-usecase: UC-C-PIC-A-003
