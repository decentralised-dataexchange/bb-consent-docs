description: READ - Individual ID supplied as HTTP header. Fetches the current ConsentRecord
  for an Agreement. There should be one unambiguous ConsentRecord for an Individual
  and an Agreement.
operationId: serviceReadIndividualRecordRead
parameters:
- description: Unique ID of an object
  in: path
  name: dataAgreementId
  required: true
  schema:
    type: string
responses:
  '200':
    content:
      application/json:
        schema:
          $ref: ../definitions/ConsentRecord.yaml
    description: ''
  '400':
    description: bad input parameter
security:
  - BearerAuth: []
summary: READ - Individual ID supplied as HTTP header. Fetches the current ConsentRecord
  for an Agreement. There should be one unambiguous ConsentRecord for an Individual
  and an Agreement.
tags:
- service
x-specification-crudl-model: ''
x-specification-pii-or-sensitive: 'True'
x-specification-scenario: ''
x-specification-usecase: UC-C-PIC-I-001
