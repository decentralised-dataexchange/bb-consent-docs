get:
  tags:
    - onboarding
  description: Get organization image
  parameters:
    - name: organizationId
      in: path
      required: true
      schema:
        type: string
    - name: imageId
      in: path
      required: true
      schema:
        type: string
  responses:
    "200":
      headers: {}
  deprecated: false
  security:
    - BearerAuth: []
